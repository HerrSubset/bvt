#!/usr/bin/env ruby

require 'bvt'

fed = Bvt.load_federation(ARGV[0])

if fed != nil
  puts "The name of the loaded federation is #{fed.name}"
  puts "It has #{fed.leagues.length} leagues"
else
  puts "This federation was not found"
end
